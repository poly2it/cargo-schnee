# Bench artefacts. Regenerated by `nix run .` on every successful run.
result
nixos.qcow2

# Captured by the host runner only on failure (post-VM /results/build.log
# is copied here as a post-mortem). Not source-of-truth — re-run the bench.
build.log
